{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capabilities for the main window: core IPC and window operations. Repository and filesystem access happens exclusively through vava's own commands, never through broad plugins.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
